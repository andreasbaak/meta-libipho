# meta-libipho: Yocto layer of the Libipho (Linux-based individual photobooth) project

In this repository, we provide a Yocto layer that is used to build the
operating system for the Libipho project.
